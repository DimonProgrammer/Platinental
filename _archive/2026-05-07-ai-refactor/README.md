# Archive: 2026-05-07 AI Filesystem Refactor

This archive preserves source-like material removed from the active production tree during the AI-oriented filesystem refactor.

## Contents
- `concepts/`: old public `/concept/*` design prototypes and their components.
- `legacy-src/`: pre-site layout, components, and styles no longer used by production pages.
- `unused-src-assets/`: images that were in `src/assets/photos` but are not imported by active pages/components.
- `public-backups/`: old public backup assets removed from deploy output.
- `qa-scripts/`: ad hoc root QA scripts superseded by direct npm/build/Playwright checks.
- `source-assets/`: raw brand, doctor, before/after, and photo source material.
- `notes/`: raw client notes, briefs, copied prompt material, and old source spreadsheets.

## Rules
- Do not import from this directory.
- Do not add generated build output here.
- Restore files to active paths only with a deliberate product or engineering decision.
