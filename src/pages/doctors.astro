---
import V3Layout from '../layouts/V3Layout.astro';
import Navigation from '../components/v3/sections/Navigation.astro';
import PageHero from '../components/v3/sections/PageHero.astro';
import FinalCTA from '../components/v3/sections/FinalCTA.astro';
import Footer from '../components/v3/sections/Footer.astro';
import { doctors } from '../data/doctors';

const allDoctors = doctors;
---

<V3Layout
  title="Врачи — Platinental Казань"
  description="Пластические хирурги и косметологи клиники Platinental Казань. Образование, специализации, стоимость консультации."
>
  <Navigation />
  <main id="main">
    <PageHero
      pageMarker="04 / 04"
      pageMarkerLabel="Команда"
      eyebrow="Команда"
      h1="Врачи клиники"
      lead="Каждый врач — мастер в своём деле. Приходите на консультацию — подберём нужного специалиста под вашу задачу."
      ctaPrimary={{ label: 'Записаться на консультацию', href: '#form' }}
      meta={[
        { num: 'Сеченов · РНЦХ', label: 'ординатура хирургов' },
        { num: 'Исключительная', label: 'квалификация' },
        { num: 'авторские', label: 'методики и патенты' },
        { num: 'с 2011', label: 'в Казани' },
      ]}
    />

    <section class="v3-doctors v3-surface-cream" aria-labelledby="doctors-heading">
      <div class="v3-container v3-container-wide">
        <header class="doctors-header" data-v3-stagger>
          <span class="v3-eyebrow">Команда</span>
          <h2 id="doctors-heading" class="v3-h2">Врачи клиники</h2>
        </header>
        <ul class="doctors-grid">
          {allDoctors.map((d) => (
            <li class="doctor-card" data-v3-reveal>
              <a href={`/doctors/${d.slug}`} class="doctor-link">
                <div class="doctor-photo">
                  {d.photo ? (
                    <img src={d.photo} alt={d.name} width="400" height="500" loading="lazy" />
                  ) : (
                    <div class="doctor-initials" aria-hidden="true">{d.initials}</div>
                  )}
                </div>
                <div class="doctor-info">
                  <p class="doctor-spec v3-eyebrow">{d.spec}</p>
                  <h3 class="doctor-name v3-h3">{d.name}</h3>
                  <p class="doctor-short v3-body" set:html={d.short}></p>
                  <div class="doctor-price">
                    <span class="price-primary v3-caption">{d.priceMain}</span>
                    {d.priceSub && <span class="price-sub v3-caption">{d.priceSub}</span>}
                  </div>
                </div>
              </a>
            </li>
          ))}
        </ul>
      </div>
    </section>

    <FinalCTA
      heading="Не знаете,<br />к кому записаться?"
      lead="Если вы не знаете, к какому врачу записаться — оставьте заявку, администратор подберёт специалиста под вашу задачу."
    />
  </main>
  <Footer />
</V3Layout>

<style>
  .v3-doctors {
    padding-top: var(--v3-gap-deep);
    padding-bottom: var(--v3-gap-deep);
  }
  .doctors-header {
    display: flex;
    flex-direction: column;
    gap: var(--v3-gap-eyebrow-h1);
    margin-bottom: var(--v3-gap-small);
  }
  .doctors-grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2px;
    background: var(--v3-border-hair);
  }
  .doctors-grid-cosm {
    grid-template-columns: repeat(2, 1fr);
    max-width: 48rem;
  }
  .doctor-card {
    background: var(--v3-cream);
  }
  .v3-surface-sand .doctor-card {
    background: var(--v3-sand);
  }
  .doctor-link {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    height: 100%;
    transition: background var(--v3-duration-fast) var(--v3-ease-out);
  }
  .doctor-link:hover { background: var(--v3-cream-alt); }
  .v3-surface-sand .doctor-link:hover { background: var(--v3-sand-deep); }

  .doctor-photo {
    aspect-ratio: 1/1;
    overflow: hidden;
    background: var(--v3-sand);
    position: relative;
    border-radius: 50%;
    margin: 1.5rem 1.5rem 0;
  }
  .doctor-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    transition: transform 0.6s var(--v3-ease-out);
  }
  .doctor-link:hover .doctor-photo img {
    transform: scale(1.04);
  }
  .doctor-initials {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--v3-font-display);
    font-style: normal;
    font-size: clamp(2.5rem, 5vw, 4rem);
    color: var(--v3-ink-muted);
  }

  .doctor-info {
    padding: 1.25rem 1.5rem 1.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    flex: 1;
  }
  .doctor-spec { color: var(--v3-ink-muted); }
  .doctor-name { color: var(--v3-ink); }
  .doctor-short { color: var(--v3-ink-secondary); margin: 0; font-size: 0.9375rem; line-height: 1.5; }
  .doctor-price {
    display: flex;
    flex-direction: column;
    gap: 0.125rem;
    margin-top: auto;
    padding-top: 0.75rem;
  }
  .price-primary { color: var(--v3-champagne-deep); text-transform: none; letter-spacing: 0.005em; font-family: var(--v3-font-display); font-style: normal; font-size: 0.9375rem; }
  .price-sub { color: var(--v3-ink-muted); text-transform: none; letter-spacing: 0.005em; }

  @media (max-width: 1024px) {
    .doctors-grid { grid-template-columns: repeat(2, 1fr); }
    .doctors-grid-cosm { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 560px) {
    .doctors-grid { grid-template-columns: 1fr; }
    .doctors-grid-cosm { grid-template-columns: 1fr; }
  }
</style>
